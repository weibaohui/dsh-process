{
  "name": "@weibaohui/dsh-process",
  "version": "0.4.1",
  "description": "dsh 插件 · 工艺管理：把 ntd 的「工艺」（Process，多阶段·多环节 agent 工作流模板）接进 dsh web——浏览/编辑/校验/导入导出/AI 生成工艺，内置库只读、我的库可写，文件改动实时同步；agent 可通过 process_* 工具读工艺库、按工艺分阶段推进。",
  "license": "MIT",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./client": "./client/bundle.js",
    "./package.json": "./package.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/weibaohui/dsh-process.git"
  },
  "homepage": "https://github.com/weibaohui/dsh-process#readme",
  "bugs": "https://github.com/weibaohui/dsh-process/issues",
  "files": [
    "src",
    "client",
    "scripts",
    "docs/*.gif",
    "cordis.patch.yml",
    "README.md",
    "LICENSE"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "scripts": {
    "check": "node --check src/index.js && node --check src/store.js && node --check src/validate.js && node --check src/routes.js && node --check src/tools.js && node --check src/prompt.js && node --check src/zip.js && node --check client/index.js",
    "test": "node --test \"test/*.test.mjs\"",
    "build:client": "node scripts/build-client.mjs",
    "prepublishOnly": "npm run build:client && npm run check && npm test"
  },
  "keywords": [
    "dsh",
    "deepseek-harness",
    "dsh-plugin",
    "ntd",
    "process",
    "workflow",
    "工艺",
    "yaml"
  ],
  "dependencies": {
    "@weibaohui/dsh-plugin-kit": "^0.3.0",
    "yaml": "^2.9.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
