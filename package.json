{
  "name": "@weibaohui/dsh-process",
  "version": "0.6.7",
  "description": "dsh 插件 · 工艺管理：「工艺」是可复用的多阶段·多环节 agent 工作流模板——定义阶段、环节指令、门禁与流转规则，agent 按工艺分阶段推进。支持浏览/编辑（表单与 YAML 双向同步）/校验/导入导出/AI 生成，内置库只读、我的库可写；面板可直接发起运行，逐环节开真实会话执行、门禁评审，看板跟踪全程。",
  "license": "MIT",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./client": "./client/bundle.js",
    "./package.json": "./package.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/weibaohui/dsh-process.git"
  },
  "homepage": "https://github.com/weibaohui/dsh-process#readme",
  "bugs": "https://github.com/weibaohui/dsh-process/issues",
  "files": [
    "src",
    "client",
    "scripts",
    "cordis.patch.yml",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20",
    "dsh": ">=0.1.7-rc.2"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "scripts": {
    "check": "node --check src/index.js && node --check src/store.js && node --check src/validate.js && node --check src/routes.js && node --check src/tools.js && node --check src/prompt.js && node --check src/zip.js && node --check src/artifacts.js && node --check src/runs.js && node --check src/execution.js && node --check client/index.js",
    "test": "node --test \"test/*.test.mjs\"",
    "build:client": "node scripts/build-client.mjs",
    "prepublishOnly": "npm run build:client && npm run check && npm test"
  },
  "keywords": [
    "dsh",
    "deepseek-harness",
    "dsh-plugin",
    "process",
    "workflow",
    "工艺",
    "yaml"
  ],
  "dependencies": {
    "@weibaohui/dsh-plugin-kit": "0.3.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
